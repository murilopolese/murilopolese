{{!< default}}

<div class="container">
	<div class="row">
		<div class="col">
			{{{entry.content.html}}}
		</div>
	</div>
	{{#if entries}}
		{{#each entries}}
			<div class="row py-4">
				<div class="col">
					{{#if page.images.[0]}}
						<div class="mb-3 thumbnail" style="width:100%;height:200px;background: url('{{page.images.[0].secure_url}}');background-size: cover; background-position: center center;">
							<a class="thumbnail-clickable" href="/{{slug}}"></a>
						</div>
					{{/if}}
					<h3><a href="/{{slug}}">{{title}}</a></h3>
					<h6>{{date createdAt}}</h6>
					{{{page.brief.html}}}
					<p>
						<a href="/{{slug}}">Read more</a>
					</p>
				</div>
			</div>
		{{/each}}
	{{else}}
		<div class="row">
			<div class="col">
				There are no entries yet.
			</div>
		</div>
	{{/if}}
</div>
